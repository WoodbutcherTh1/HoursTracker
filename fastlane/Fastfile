default_platform(:ios)

platform :ios do
  desc "Capture Apple Watch App Store screenshots (en/he/ar × Series 11 + Ultra 3)"
  lane :watch_screenshots do
    sh("bash", File.expand_path("../scripts/capture_watch_screenshots.sh", __dir__))
  end
end
